# Use the README file as a reference to customize settings.

Notice:
  fields:
    default: data, token, user_id, question, status, google_auth_connection_id
    form: data, token, user_id, question, status, google_auth_connection_id
  relationships: openings, policy
  application: Application
