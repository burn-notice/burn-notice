- set_title('New Notice')
- set_crumbs('Notices' => notices_path, 'New Notice' => url_for)

= form_errors @notice
= form_for @notice, url: share_notice_path(@notice), html: {class: "form-horizontal"} do |form|
  = render 'share_form', notice: @notice, form: form
